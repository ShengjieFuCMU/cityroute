[pytest]
# Only collect tests from the dedicated folder (ignore _scratch_* files elsewhere)
testpaths = tests

# Add the backend repo root to sys.path so "services.*", "utils.*", etc. import cleanly
pythonpath = .
filterwarnings =
    ignore:The 'app' shortcut is now deprecated:DeprecationWarning:httpx\._client

